package me.samfreeman.java.BigNumber;

import java.math.BigDecimal;
import java.util.Scanner;

public class JavaBigDecimal {
	public static void main(String[] args) {
		// Input
		Scanner sc = new Scanner(System.in);
		int n = sc.nextInt();
		String[] s = new String[n + 2];
		for (int i = 0; i < n; i++) {
			s[i] = sc.next();
		}
		sc.close();
		
		BigDecimal[] compareArr = new BigDecimal[n];
		for (int i = 0; i < n; i++) {
			compareArr[i] = BigDecimal.valueOf(Double.parseDouble(s[i]));
		}
		for (int i = 0; i < n; i++) {
			int j = i - 1;
			BigDecimal x = compareArr[i];
			while (j >= 0 && compareArr[j].compareTo(x) < 0) {
				compareArr[j + 1] = compareArr[j];
				j--;
			}
			compareArr[j + 1] = x;
		}

		// Output
		for (int i = 0; i < n; i++) {
			System.out.println(compareArr[i]);
		}
	}
}
